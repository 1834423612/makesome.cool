/*
 *************************
 *     已使用的子域名     *
 *************************
 *
 * 本文件内子域名目前均已启用
 * 在下方添加一个你想使用的的域名，并提交一个 PR 
 *
 *
 *************************
 *         说明          *
 *************************
 *
 * KEY:         你提交的子域名（如：`test` 表示域名为 `test.makesome.cool`）
 *
 * VALUE:       以 Github 为例，`test.github.io` 表示用户/组织的主页，
 *              `test.github.io/project` 表示项目主页，也可以绑定 Gitee、 CODING.NET、vercel 等其它托管服务商。
 *
 * 注意:        目前 MAKESOME.COOL 使用的 DNS 服务商为 DNSPod专业版。
 *              该服务商目前不支持添加 NS 域名记录，
 *
 */

active.CNAME = {
  // 请在此处区域内添加自定义域名

  // 请在此行之上新增一行后提交 Pull Request，不要忘记再提交 issue
  // 示例：
  // 'youarname': 'username.github.io',
  // 以下为已启用的保留域名，请勿修改
  '@': 'test.com',
  'www': 'test.com',
};